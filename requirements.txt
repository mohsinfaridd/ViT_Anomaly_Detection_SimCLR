# Core scientific stack (versions from executed final notebooks where known)
# Reviewer / notebook environment: NVIDIA A100-SXM4-80GB, CUDA 12.8, PyTorch 2.11.0+cu128
#
# Install a CUDA-matched torch/torchvision build from https://pytorch.org/get-started/locally/
# before or instead of the unpinned torch line below.

torch
torchvision
timm
numpy
pandas
scipy
scikit-learn
Pillow
matplotlib
tqdm
PyYAML

# Tests
pytest
